# Go
/platform
/bin/
*.test
*.out
coverage.txt

# Node / dashboard
node_modules/
dashboard/dist/
dashboard/.vite/

# Playwright (transient e2e output)
dashboard/test-results/
dashboard/playwright-report/
dashboard/.playwright/
dashboard/e2e/.run-admin.json
dashboard/e2e/.run-admin-state.json

# Embedded SPA (built into the binary at image-build time)
internal/webdist/static/

# Env & secrets
.env
!.env.example

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Build artifacts
dist/
tmp/

# Plugin working copies. Each plugin is its own repository — an in-tree plugin could
# reach core sources and lean on a `replace`, which is exactly what the v0.3 exit
# criterion exists to catch (docs/v0.3/05-first-party-plugins.md, Decision log).
plugins/
