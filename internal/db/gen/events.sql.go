// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: events.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countEvents = `-- name: CountEvents :one
SELECT count(*) FROM events
`

func (q *Queries) CountEvents(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countEvents)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createEvent = `-- name: CreateEvent :one
INSERT INTO events (id, name, description, starts_at, ends_at, freeze_at)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, name, description, starts_at, ends_at, freeze_at, created_at, updated_at
`

type CreateEventParams struct {
	ID          uuid.UUID
	Name        string
	Description string
	StartsAt    time.Time
	EndsAt      time.Time
	FreezeAt    *time.Time
}

func (q *Queries) CreateEvent(ctx context.Context, arg CreateEventParams) (Event, error) {
	row := q.db.QueryRow(ctx, createEvent,
		arg.ID,
		arg.Name,
		arg.Description,
		arg.StartsAt,
		arg.EndsAt,
		arg.FreezeAt,
	)
	var i Event
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.StartsAt,
		&i.EndsAt,
		&i.FreezeAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getEvent = `-- name: GetEvent :one
SELECT id, name, description, starts_at, ends_at, freeze_at, created_at, updated_at FROM events ORDER BY created_at ASC LIMIT 1
`

func (q *Queries) GetEvent(ctx context.Context) (Event, error) {
	row := q.db.QueryRow(ctx, getEvent)
	var i Event
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.StartsAt,
		&i.EndsAt,
		&i.FreezeAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateEvent = `-- name: UpdateEvent :one
UPDATE events SET
    name        = coalesce($2, name),
    description = coalesce($3, description),
    starts_at   = coalesce($4, starts_at),
    ends_at     = coalesce($5, ends_at),
    freeze_at   = CASE WHEN $6::boolean
                       THEN $7
                       ELSE freeze_at END,
    updated_at  = now()
WHERE id = $1
RETURNING id, name, description, starts_at, ends_at, freeze_at, created_at, updated_at
`

type UpdateEventParams struct {
	ID          uuid.UUID
	Name        *string
	Description *string
	StartsAt    *time.Time
	EndsAt      *time.Time
	SetFreeze   bool
	FreezeAt    *time.Time
}

func (q *Queries) UpdateEvent(ctx context.Context, arg UpdateEventParams) (Event, error) {
	row := q.db.QueryRow(ctx, updateEvent,
		arg.ID,
		arg.Name,
		arg.Description,
		arg.StartsAt,
		arg.EndsAt,
		arg.SetFreeze,
		arg.FreezeAt,
	)
	var i Event
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.StartsAt,
		&i.EndsAt,
		&i.FreezeAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
